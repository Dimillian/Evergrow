# Inventory management captures

Captured in the Codex in-app browser at 1524 × 1272 on 2026-09-06. These are actual local review screenshots, using the real inventory panel and renderer with memory-only character data. No gameplay simulation or character saves were used.

- `inventory-buttons.png`: compact Sort & filter and Equip Best icons beside Inventory.
- `sort-filter.png`: Weapons + Armor and Rare + Epic selected together in the compact popover.
- `filtered-grid.png`: those two matching items, the complete 8×8 grid, and the match count below it.
- `equip-best-warning.png`: a staged staff loadout with a higher-power sword in the bag; the three choices are shown before any equipment commit.

The first three captures use `/character.html?comparison=twohand`; open Sort & filter and select the four filters for the latter two. The warning uses `/character.html?loadout=staff`, then Equip Best. Captures show UI states, not a hardware gamepad acceptance test.
