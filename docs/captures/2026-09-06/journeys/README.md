# Journeys review

2026-09-06. Captured in the Codex in-app browser at 1280 × 720 using `/journeys.html?view=hud`, `view=journal` and `view=crypt`.

These are the actual shared UI components over a frozen seeded world, with staged activity data and memory-only exploration. No gameplay ticks or character save access. Captures show presentation, not gameplay validation.

`sidebar-refined.png` supersedes the original mini-log presentation: aligned map/portal/log edges, compact portal row and quieter activity hierarchy. The original captures remain an earlier visual checkpoint.

`sidebar-minimal.png` and `journal-minimal.png` supersede the earlier sidebar: consistent activity names, single-line entries, quiet difficulty numerals and metre/kilometre distances. `completion.png` stages the shared objective XP announcement.
